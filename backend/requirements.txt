# Runtime dependencies for the Vercel Python deployment. Vercel's Python
# runtime installs from this file (it does not read pyproject.toml). Kept in
# sync with pyproject.toml [project.dependencies] — update both together.
#
# uvicorn is intentionally omitted: Vercel invokes the ASGI app directly, so no
# server process is needed here. It stays in pyproject for local/Docker runs.
fastapi>=0.115
pydantic-settings>=2.4
pyjwt[crypto]>=2.9
httpx>=0.27
pymupdf>=1.24
python-docx>=1.1
mammoth>=1.8
python-multipart>=0.0.12
reportlab>=4.2
